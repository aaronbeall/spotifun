// Shared color themes for the Spectrum/Flow/Popularity cards, used both by
// the live app cards' background treatment and by their share-card visuals,
// so the two stay visually in sync.
export const CARD_THEMES = {
  spectrum: { light: '#60a5fa', dark: '#1e3a8a' },
  flow: { light: '#f472b6', dark: '#831843' },
  popularity: { light: '#facc15', dark: '#713f12' },
};
